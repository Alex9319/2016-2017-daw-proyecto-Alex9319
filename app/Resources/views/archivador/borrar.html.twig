{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Confirmar eliminación de {{ archivador }}</h1>
    <form method="post" action="{{ path('confirmar_borrar_archivador', {'id': archivador.id}) }}" name="confirmar">
        <button type="submit" name="eliminar" class="btn btn-danger"><i class="fa fa-trash"></i> Sí, quiero eliminarlo</button>
        <a href="javascript:history.go(-1);" class="btn btn-info"><i class="fa fa-reply"></i> No, quiero volver</a>
    </form>
{% endblock %}

