{% extends 'layout.html.twig' %}

{% block content %}
    <h1>
        {% if usuario.id %}
            Modificar usuario {{ usuario.usuario }}
        {% else %}
            Nuevo usuario
        {% endif %}
    </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">

        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}

